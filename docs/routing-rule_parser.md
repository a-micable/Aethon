## Tuning 1

For `RuleParser`, tune thresholds from archived captures before enabling them on live traffic. The useful signal is rule validation; values should be reviewed with packet loss and relay priority in mind.
