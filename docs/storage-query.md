## Tuning 1

For `ArchiveQuery`, tune thresholds from archived captures before enabling them on live traffic. The useful signal is predicate filtering; values should be reviewed with packet loss and relay priority in mind.
